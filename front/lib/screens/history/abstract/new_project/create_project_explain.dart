import 'package:flutter/material.dart';

class CreateProjectExplain extends StatelessWidget {
  final AnimationController animationController;
  const CreateProjectExplain({Key? key, required this.animationController})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
